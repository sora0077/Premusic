# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iTunesService' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iTunesService

end

target 'Premusic' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Premusic
  pod 'SwiftLint'

  # plugin 'cocoapods-keys', {
  #     :project => "Premusic",
  #     :keys => [
  # ]}

end

target 'PremusicCore' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PremusicCore

  target 'PremusicCoreTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
